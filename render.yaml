services:
  - type: web
    name: resume-builder
    env: python
    buildCommand: apt-get install -y texlive-latex-base texlive-fonts-recommended texlive-latex-extra && pip install flask
    startCommand: python app.py
    plan: free
